require 'rubygems'
require 'rake'
require 'echoe'

Echoe.new('gater', '0.0.1') do |p|
  p.description    = "Support for logical feature gating in ruby / rails"
  p.url            = "http://github.com/tomheon/gater"
  p.author         = "Greg Belote and Edmund Jorgensen"
  p.email          = "tomheon @nospam@ gmail.com and etoleb @nospam@ gmail.com"
  p.ignore_pattern = ["script/*", "example.rb", "gates.yml"]
  p.development_dependencies = []
end
